// Intentionally empty — the backend UI is served from the FastAPI app
// itself, so the shell doesn't need to expose any Node APIs to the page.
// Kept so `contextIsolation: true` has a valid preload path.
